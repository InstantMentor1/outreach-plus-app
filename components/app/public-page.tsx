import Navbar from '@/components/navbar';
type Section={eyebrow:string;title:string;copy:string;items?:string[]};
export function PublicPage({ title, intro, sections }: { title:string; intro:string; sections:Section[] }) { return <><Navbar/><main className="public-page"><section className="public-hero"><p className="app-eyebrow">Outreach+ for hospitality</p><h1>{title}</h1><p>{intro}</p><a href="/register" className="public-cta">Join Founding Access</a></section>{sections.map(section=><section className="public-section" key={section.title}><p className="app-eyebrow">{section.eyebrow}</p><h2>{section.title}</h2><p>{section.copy}</p>{section.items&&<ul>{section.items.map(item=><li key={item}>{item}</li>)}</ul>}</section>)}</main></> }
