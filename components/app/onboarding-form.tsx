'use client';
import { FormEvent, useState } from 'react';
export function OnboardingForm(){const[step,setStep]=useState(1),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setLoading(true);setError('');const data=Object.fromEntries(new FormData(event.currentTarget));const response=await fetch('/api/onboarding',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const result=await response.json().catch(()=>({}));setLoading(false);if(response.ok)location.assign('/dashboard');else setError(result.error||'We could not save your profile.')}return <form className="onboarding-form" onSubmit={submit}><div className="onboarding-progress"><span className={step>=1?'active':''}>1 Links</span><span className={step>=2?'active':''}>2 Business</span><span className={step>=3?'active':''}>3 Brand</span></div><section hidden={step!==1}><h2>Where can we learn about your business?</h2><p>Optional links help your team add context later. Automatic extraction is not enabled in this version.</p><label>Website URL<input name="website" type="url" placeholder="https://"/></label><label>Instagram handle<input name="instagram" placeholder="@yourcafe"/></label><label>Google Business Profile link<input name="google" type="url" placeholder="https://"/></label><button type="button" onClick={()=>setStep(2)}>Continue</button></section><section hidden={step!==2}><h2>Tell us the essentials.</h2><label>Business name<input name="businessName" required/></label><label>Business category<select name="category" required><option value="">Select</option><option>Café</option><option>Restaurant</option><option>Cloud kitchen</option><option>Small hospitality brand</option></select></label><label>Location<input name="location" required/></label><label>Primary marketing goal<input name="goal" placeholder="Increase weekday sales" required/></label><label>Main customer type<input name="audience" placeholder="Students and nearby professionals" required/></label><button type="button" onClick={()=>setStep(3)}>Continue</button></section><section hidden={step!==3}><h2>Review your initial brand profile.</h2><p>This is a manual fallback. You can improve these details anytime before campaign generation.</p><label>Brand tone<input name="tone" placeholder="Warm, local and conversational"/></label><div className="notice"><b>What happens next</b><p>Outreach+ uses these approved details to recommend a relevant first campaign. You always review campaign content before it is used.</p></div>{error&&<p className="form-error">{error}</p>}<button disabled={loading}>{loading?'Saving...':'Save profile and open dashboard'}</button></section></form>}
