const memory=['Logo','Colors','Typography','Brand voice','Products','Services','Audience','Approved creatives','Rejected creatives','Past campaigns','Client preferences']; export default function BrandMemory(){return <section className="section memory"><div className="shell split"><div className="brain"><div className="brain-core">BRAND<br/><b>BRAIN</b></div>{memory.map((x,i)=><span className={`memory-tag m${i}`} key={x}>{x}</span>)}</div><div><p className="kicker">PERSISTENT CONTEXT</p><h2>Your brand is remembered.</h2><p className="lede">Every preference, decision and approved campaign gives Outreach+ a clearer picture of what great looks like for you.</p><p className="big-quote">“No more repeating the same brief.”</p></div></div></section>}
