'use client';
import { FormEvent, useState } from 'react';
export function AdminLoginForm() { const [error, setError] = useState(''); const [pending, setPending] = useState(false); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setPending(true); setError(''); const password = new FormData(event.currentTarget).get('password'); const response = await fetch('/api/admin/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ password }) }); if (response.ok) location.assign('/admin'); else setError('Sign-in was not successful. Check your password and try again.'); setPending(false); } return <form className="portal-form" onSubmit={submit}><label>Password<input name="password" type="password" autoComplete="current-password" required /></label>{error && <p className="portal-error" role="alert">{error}</p>}<button disabled={pending}>{pending ? 'Signing in...' : 'Sign in'}</button></form>; }
