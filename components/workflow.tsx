const steps=[['01','Tell us','Send a message, note or reference.'],['02','Understand','We read the request and your brand.'],['03','Clarify','One smart question, if needed.'],['04','Create','Strategy, copy and creative — together.'],['05','Approve','Give feedback in the same chat.'],['06','Publish','Live where your audience is.']]; export default function Workflow(){return <section id="how" className="section shell"><p className="kicker">THE WORKFLOW</p><h2>From one message to a finished campaign.</h2><div className="timeline">{steps.map(([n,title,text])=><article key={n}><span>{n}</span><div className="tiny-ui">{n==='04'?'✦':n==='06'?'↗':'●'}</div><h3>{title}</h3><p>{text}</p></article>)}</div></section>}
