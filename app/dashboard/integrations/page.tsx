const rows=[['WhatsApp','Available when Meta Cloud API is connected','Conversation requests and replies'],['Instagram','Not connected','Publishing and performance data'],['Facebook','Not connected','Publishing and performance data'],['Google Business Profile','Not connected','Review and local visibility signals'],['POS / PMS / CRM','Coming later','Revenue, occupancy and customer context']]; export default function Integrations(){return <><p className="app-eyebrow">Integrations</p><h1>Connect only what Outreach+ can use.</h1><section className="integration-list">{rows.map(([name,status,copy])=><article key={name}><div><h3>{name}</h3><p>{copy}</p></div><span>{status}</span><button disabled={!status.startsWith('Available')}>Connect</button></article>)}</section></>}
