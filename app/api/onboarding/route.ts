import { NextRequest, NextResponse } from 'next/server';
import { getUserSession } from '@/lib/server/auth';
import { safeText, sameOrigin } from '@/lib/server/security';
import { supabaseAdmin } from '@/lib/server/supabase';
export async function POST(request: NextRequest){if(!sameOrigin(request))return NextResponse.json({error:'Invalid request.'},{status:403});const session=await getUserSession();if(!session)return NextResponse.json({error:'Please log in again.'},{status:401});let body:Record<string,unknown>={};try{body=await request.json()}catch{}const businessName=safeText(body.businessName,120),category=safeText(body.category,80),location=safeText(body.location,120),goal=safeText(body.goal,200),audience=safeText(body.audience,200),website=safeText(body.website,240),instagram=safeText(body.instagram,120),google=safeText(body.google,240),tone=safeText(body.tone,200);if(!businessName||!category||!location||!goal||!audience)return NextResponse.json({error:'Complete the required business details.'},{status:400});try{const db=supabaseAdmin();const {data:client,error}=await db.from('clients').upsert({owner_user_id:session.userId,business_name:businessName,business_type:category,location,website_url:website||null,status:'ready'},{onConflict:'owner_user_id'}).select('id').single();if(error||!client)throw error;const {error:profileError}=await db.from('brand_profiles').upsert({client_id:client.id,business_summary:`${businessName} is a ${category} in ${location}.`,target_audience:audience,tone_of_voice:tone||null,additional_context:{marketing_goal:goal,instagram:instagram||null,google_business_link:google||null,source:'manual onboarding'},reviewed:false},{onConflict:'client_id'});if(profileError)throw profileError;return NextResponse.json({ok:true})}catch{return NextResponse.json({error:'Your business profile could not be saved. Check the Supabase setup.'},{status:500})}}
