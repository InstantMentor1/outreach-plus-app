import Navbar from '@/components/navbar';
import '../public-pages.css';
import '../industry-pages.css';

const placements = ['Table tents', 'Bills and receipts', 'Takeaway packaging', 'Delivery bags', 'Store posters', 'Instagram stories', 'WhatsApp messages', 'Partner locations'];
const actions = ['Claim offer', 'View menu', 'Book table', 'Order now', 'Join WhatsApp list', 'Leave a review'];
export default function SmartQrPage() { return <><Navbar /><main className="public-page"><p className="app-eyebrow">Outreach+ Smart QR · Product preview</p><h1>From campaign visibility to measurable action.</h1><p className="public-intro">Smart QR connects posters, packaging and messages to offers, bookings and customer responses that Outreach+ can track when this capability is enabled.</p><section className="public-block"><h2>How it works</h2><ol><li>Outreach+ creates the campaign.</li><li>The business selects one customer action.</li><li>A unique campaign QR code is generated.</li><li>The QR is placed on a poster, package or message.</li><li>Customers scan and complete the action.</li><li>Outreach+ reports supported scans, claims and validated redemptions.</li></ol></section><section className="public-block"><h2>Hospitality actions first</h2><div className="pill-grid">{actions.map((action) => <span key={action}>{action}</span>)}</div></section><section className="public-block"><h2>Placement options</h2><div className="pill-grid">{placements.map((placement) => <span key={placement}>{placement}</span>)}</div></section><section className="public-block preview"><small>COMING SOON · PRODUCT PREVIEW</small><h2>Campaign reporting</h2><div className="preview-grid">{['Total scans', 'Unique scans', 'Offer claims', 'Validated redemptions', 'Conversion rate', 'Placement source', 'Campaign date range'].map((metric) => <div key={metric}>{metric}<b>Pending tracking setup</b></div>)}</div></section><section className="public-block"><small>EARLY ACCESS</small><h2>Partner Rewards</h2><p>Reward approved local partners for measurable customers they send. Unique partner QR codes, redemption validation and rewards are a future capability. Outreach+ does not currently process payouts or wallets.</p></section></main></>; }
