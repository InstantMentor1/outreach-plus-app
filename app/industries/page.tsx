import EarlyAccessForm from '@/components/early-access-form';
import Navbar from '@/components/navbar';
import { industryPlaybooks } from '@/lib/industry-playbooks';
import '../public-pages.css';
import '../industry-pages.css';

export default function IndustriesPage() { return <><Navbar /><main className="public-page"><p className="app-eyebrow">Industry playbooks</p><h1>One campaign engine. Built around your business.</h1><p className="public-intro">Outreach+ applies a shared campaign engine with sector-specific objectives, offers, creative direction and customer actions. Hospitality is available now; local commerce playbooks are expanding carefully.</p><section className="industry-grid">{industryPlaybooks.map((item) => <article key={item.slug}><small className={item.status}>{item.status === 'available' ? 'HOSPITALITY PLAYBOOK · AVAILABLE NOW' : 'PLAYBOOK COMING SOON'}</small><h2>{item.name}</h2><p><b>Goal:</b> {item.goal}</p><p><b>Recommendation:</b> {item.recommendation}</p><p><b>Customer action:</b> {item.action}</p>{item.status === 'available' ? <a href="/register">Create campaign</a> : <a href="#early-access">Join early access</a>}</article>)}</section><section className="industry-table"><h2>How playbooks adapt</h2><div className="industry-row industry-head"><b>Industry</b><b>Goal</b><b>Action</b><b>Example</b></div>{industryPlaybooks.map((item) => <div className="industry-row" key={item.slug}><strong>{item.name}</strong><span>{item.goal}</span><span>{item.action}</span><span>{item.status === 'available' ? item.example : 'Coming soon'}</span></div>)}</section><section id="early-access" className="public-block early-access-block"><p className="app-eyebrow">Early access</p><h2>Tell us which playbook you need.</h2><p>Share your business name, work email and website. We will keep your request on the waitlist and send an email when that playbook is ready.</p><EarlyAccessForm industry="Industry playbook" /></section></main></>; }
